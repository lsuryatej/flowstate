Resolves # 

## Layer Changed
- [ ] Webview (React)
- [ ] Sidecar (Node/TypeScript)
- [ ] Host (Rust/Tauri)

## Checks
- [ ] `npx tsc -b` (or `--noEmit` for old config) passes
- [ ] `cd src-tauri && cargo check` passes
